[project]
name = "fastapi-example"
version = "0.1.0"
description = "FastAPI with HTML and JS"
authors = [
    { name = "RaZel713", email = "razel7130@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "jinja2"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
